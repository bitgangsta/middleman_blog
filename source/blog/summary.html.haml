%h3
    %i.icon-chevron-right
    Recent Blog Posts
- blog.articles[0...3].each do |article|
    %article.summary
        %h3
            %a{href: article.url}
                = article.title
        %span= article.date.strftime('%b %e, %Y')
        %p= article.summary
